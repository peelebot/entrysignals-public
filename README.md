# EntrySignals

A read-only public feed of publicly available YouTube crypto commentary.

- `index.html` / `styles.css` / `app.js` — static viewer
- `signals.json` — data snapshot (updated on demand)

All video content links directly to YouTube. This site does not host or produce trading advice.
